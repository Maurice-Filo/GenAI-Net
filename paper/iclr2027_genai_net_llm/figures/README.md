# Figure inventory

All numerical figures derived from pre-template-mask LLM campaigns were moved to
the external quarantine on 1 September 2026. They are not submission assets and
must not be copied back into this directory.

The only active figure is `architecture.pdf` (with its PNG preview), which shows
the implementation-level HOF exchange and SIL replay path without asserting a
numerical outcome. Its wording must describe one isolated Harness workspace with
a Decider call followed by a Writer call per round.

Replacement numerical figures will be generated only from contract-v2 artifacts
listed in `../EXPERIMENT_RECOVERY_PLAN.md`. Their fixed palette is RL blue, full
duplex green, direct LLM or request-0 context orange, and independent/no
communication gray. Diversity plots must be conditioned on quality, and emitter
labels must not be interpreted as ancestry.
